import { Typography } from "@mui/material";


const ScoreHeading = ({ title }) => {
	return (
		<Typography variant="h3" component="h3" >
			{title}
		</Typography>
	);
};

export default ScoreHeading;
